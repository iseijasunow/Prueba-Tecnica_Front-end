export interface UserDetail {
    id: string,
    login: string,
    avatar_url: string,
    blog: string,
    followers: number,
}

export interface UserList {
    id: string,
    login: string,
    followers: number,
}