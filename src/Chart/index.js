import { ContaierCharts, ChartTitle } from "../styles";
//import { Line } from "@ant-design/charts";

const UserChart = (data) => {
  return (
    <ContaierCharts>
      <ChartTitle>Cantidad de followers de los usuarios</ChartTitle>
    </ContaierCharts>
  );
};

export default UserChart;
